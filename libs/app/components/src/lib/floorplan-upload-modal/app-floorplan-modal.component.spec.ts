import { appFloorplanModalComponent } from './app-floorplan-modal.component';

describe('FloorplanModalComponent', () => {
  it('should create', () => {
    expect(appFloorplanModalComponent()).toEqual('app-floorplan-modal-component');
  });
});