import { appFloorplanComponent } from './app-floorplan.component';

describe('FloorplanEditorPageComponent', () => {
  it('should create', () => {
    expect(appFloorplanComponent()).toEqual('app-floorplan-component');
  });
});
